import React, { Component } from 'react';

import './styles.scss';

export default class AverageMovie extends Component {
    render() {
        return (
            <div>
                <button className="average-value">10%</button>               
            </div>
        );
    }
}